terraform {
  backend "azurerm" {
    resource_group_name  = "terraformstorage-rg"
    storage_account_name = "tfstateaccountharit2"
    container_name       = "tfstate"
    key                  = "site2.terraform.tfstate"
  }
}
